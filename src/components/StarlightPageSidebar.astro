---
import EvidenceRail from './EvidenceRail.astro';

const entries = Astro.locals.starlightRoute.entry.data.evidenceRail;
---

{entries.length > 0 && <div class="desktop-rail"><EvidenceRail entries={entries} /></div>}
